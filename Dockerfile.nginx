# Dockerfile для nginx (reverse proxy)
FROM nginx:alpine

# Копируем конфигурацию nginx
COPY nginx.conf /etc/nginx/conf.d/default.conf

# Копируем статические файлы фронтенда в папку front/
COPY front/ /usr/share/nginx/html/front/

# Открываем порт
EXPOSE 80

# Nginx запускается автоматически

