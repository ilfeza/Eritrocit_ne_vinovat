# BioDash - Прототип системы умной визуализации данных биохимического анализатора

## 📋 Содержание

1. [Цели](#цели)
2. [Задачи](#задачи)
3. [Реализованный функционал](#реализованный-функционал)
4. [Инструкция по развертыванию](#инструкция-по-развертыванию)
5. [Входные и выходные данные](#входные-и-выходные-данные)
6. [Ключевые аспекты](#ключевые-аспекты)
7. [Обработка](#обработка)

---

## Цели

Проект BioDash разработан для автоматизации обработки, анализа и визуализации лабораторных данных пациентов. Основные цели:

- **Автоматизация обработки** лабораторных данных из различных источников (CSV, JSON, Excel)
- **Создание временных референсных диапазонов** на основе статистического анализа данных
- **Обнаружение аномалий и выбросов** в лабораторных показателях
- **Визуализация данных** через веб-интерфейс для удобного анализа
- **Обеспечение масштабируемости** через контейнеризацию и микросервисную архитектуру

---

## Задачи

- Разработка REST API для загрузки и обработки таблиц с лабораторными данными
- Реализация модуля аналитики для предобработки данных и обнаружения аномалий
- Создание веб-интерфейса для визуализации результатов анализов
- Настройка инфраструктуры развертывания через Docker
- Обеспечение обработки различных форматов данных (CSV, JSON, Excel)
- Реализация механизма маркировки отклонений от референсных значений
- Генерация статистических отчетов по лабораторным тестам

---

## Реализованный функционал

### Backend (FastAPI)

![Диаграмма архитектуры бэкенда](data/Диаграмма%20без%20названия.drawio%20(6).png)

- **Загрузка таблиц**: Поддержка форматов CSV, JSON, Excel (xlsx, xls)
- **Управление данными**: CRUD операции для загруженных таблиц
- **API эндпоинты**:
  - `POST /api/tables/upload` - загрузка таблицы
  - `GET /api/tables/{table_id}` - получение таблицы по ID
  - `GET /api/tables/` - список всех таблиц
  - `DELETE /api/tables/{table_id}` - удаление таблицы
  - `GET /api/tables/{table_id}/pie-chart` - генерация круговой диаграммы
  - `GET /api/tables/{table_id}/reference-check` - проверка референсных значений
- **Хранение данных**: Глобальное хранилище в памяти (TableStorage)
- **CORS**: Настроен для работы с фронтендом
- **Health check**: Эндпоинт `/health` для мониторинга

### Demo API

Демонстрационный API для работы с предзагруженными данными пациентов и загруженными файлами:

- **Работа с демо данными**:
  - `GET /api/demo/download-file?demo_version={1|2}` - скачивание демо файла (версия 1 или 2)
  - `GET /api/demo/patient-data?demo_version={1|2}` - получение обработанных данных пациента из демо файла
    - Возвращает данные, сгруппированные по категориям, анализы не в норме и данные для графиков

- **Работа с данными из more_patients.csv**:
  - `GET /api/demo/patients-list` - список всех пациентов с их статистикой
  - `GET /api/demo/patient-data-by-id?patient_id={id}` - данные конкретного пациента по ID

- **Работа с данными из test_table.csv**:
  - `GET /api/demo/patients-list-from-test-table` - список пациентов из test_table.csv
  - `GET /api/demo/patient-data-from-test-table?patient_id={id}` - данные пациента из test_table.csv

- **Загрузка и работа с пользовательскими данными**:
  - `POST /api/demo/upload-patient-data` - загрузка CSV файла с данными пациента
    - Требуемые колонки: `patient_id`, `test_code`, `value`
    - Опциональные колонки: `test_name`, `unit`, `date`, `status`
  - `GET /api/demo/patients-list-from-uploaded` - список пациентов из загруженных файлов
  - `GET /api/demo/patient-data-from-uploaded?patient_id={id}` - данные пациента из загруженных файлов


### Analytics модуль

Модуль аналитики реализован в Jupyter ноутбуке [`analytics/lab_data_preprocessing.ipynb`](analytics/lab_data_preprocessing.ipynb) и содержит полный пайплайн предобработки лабораторных данных и создания временных референсных диапазонов. Пайплайн включает следующие этапы:

#### 1. Проверка и нормализация схемы данных
- Автоматическое распознавание колонок: `patient_id`, `date`, `test_name`, `value`, `sex`, `age`
- Предложение нормализованных имен колонок для унификации данных
- Поддержка различных форматов входных данных с префиксами: `am.*`, `chem.*`, `bc.*`, `cmv.*`, `infl.*`, `lip.*`
- Автоматический маппинг колонок к стандартной схеме

**Сопоставление названий анализов**: Модуль [`analytics/name_of_analysis.py`](analytics/name_of_analysis.py) использует fuzzy string matching (библиотека `rapidfuzz`) для автоматического сопоставления названий колонок из загруженных таблиц с нормализованными названиями анализов из JSON файла [`back/analytics/data.json`](back/analytics/data.json). 

Fuzzy string matching основан на алгоритмах расстояния редактирования (расстояние Левенштейна) и позволяет находить соответствия даже при различиях в написании, опечатках или разных форматах названий. Модуль использует комбинацию алгоритмов `rapidfuzz` (`ratio`, `partial_ratio`, `token_sort_ratio`, `token_set_ratio`) и выбирает максимальный score для каждого сравнения.

В JSON файле [`back/analytics/data.json`](back/analytics/data.json) собраны нормальные (референсные) значения для всех лабораторных анализов с полями:
- `id` - код теста (например, `chem.alt`)
- `name` - название теста (например, "Alanine Transaminase (ALT)")
- `min` - минимальное референсное значение
- `max` - максимальное референсное значение
- `unit` - единица измерения

Модуль выполняет многоуровневое сопоставление:
1. Прямое совпадение названий (регистронезависимо)
2. Сравнение нормализованных названий (транслитерация кириллицы, удаление спецсимволов)
3. Fuzzy matching с использованием алгоритмов `rapidfuzz`
4. Частичное совпадение подстрок

#### 2. Очистка данных
- **Удаление дубликатов**: Автоматическое обнаружение и удаление строгих дубликатов записей
- **Парсинг дат**: Преобразование различных форматов дат в единый стандарт
- **Типизация данных**: Приведение числовых полей к корректным типам (`float64`)
- **Обнаружение артефактов**:
  - Выявление нулевых значений для тестов, где 0 нереален (например, креатинин, глюкоза, гемоглобин)
  - Обнаружение экстремальных значений (>10 стандартных отклонений от медианы)
  - Маркировка подозрительных значений для дальнейшего анализа

#### 3. Статистический анализ по каждому тесту
Для каждого лабораторного теста рассчитывается полная статистика:
- **Базовые метрики**: `count`, `mean`, `median`, `std`, `min`, `max`
- **Перцентили**: `p1`, `p2.5`, `p25`, `p75`, `p97.5`, `p99`
- **Межквартильный размах**: `IQR` (Q3 - Q1)
- Статистика рассчитывается с исключением подозрительных значений для повышения точности

#### 4. Создание временных референсных диапазонов
Система создает временные референсы тремя методами (с приоритетом официальных референсов из Excel):

- **Метод 1 (рекомендуемый)**: Процентили 2.5-97.5
  - Использует 2.5-й и 97.5-й перцентили распределения значений
  - Наиболее устойчив к выбросам
  
- **Метод 2**: Median ± 2*IQR
  - Основан на медиане и межквартильном размахе
  - Подходит для данных с несимметричным распределением
  
- **Метод 3**: Mean ± 2*Std (z-score)
  - Использует среднее значение и стандартное отклонение
  - Применяется при достаточном объеме данных (≥30 значений)

**Поддержка стратификации**:
- Референсы по полу (`sex`) - при наличии данных о поле пациента
- Референсы по возрастным группам (`age_group`) - для учета возрастных особенностей
- Общие референсы для всех пациентов

**Интеграция с официальными референсами**:
- Загрузка официальных референсов из Excel файла (`data.xlsx`)
- Автоматическое объединение с временными референсами
- Приоритет официальных референсов над временными

#### 5. Маркировка отклонений (LOW/NORMAL/HIGH)
Автоматическая классификация каждого значения относительно референсного диапазона:
- **LOW** - значение ниже минимального референса
- **NORMAL** - значение в пределах референсного диапазона
- **HIGH** - значение выше максимального референса
- Учет специфичных референсов (по полу/возрасту) при их наличии
- Создание колонок `{test_code}_status` для каждого теста

#### 6. Обнаружение аномалий методами машинного обучения
Использование метода кластеризации для выявления аномалий (подсвечиваются в дашборде):

- **DBSCAN** (Density-Based Spatial Clustering of Applications with Noise) из библиотеки `scikit-learn`
  - Кластеризация для выявления точек вне кластеров (шумовые точки считаются аномалиями)
  - Автоматический подбор параметров на основе k-расстояний
  - Обнаруживает локальные аномалии на основе плотности данных
  - Эффективен для многомерных данных с различными формами кластеров

**Дополнительные возможности**:
- Использование PCA для снижения размерности при большом количестве тестов
- Нормализация данных через StandardScaler из `scikit-learn`
- Автоматический расчет оптимального параметра `eps` на основе анализа k-расстояний

#### 7. Обнаружение выбросов статистическими методами
Строгие статистические методы для выявления выбросов (рекомендуется удалять):

- **IQR (строгий)**: Q1 - 3*IQR, Q3 + 3*IQR
  - Более строгий критерий, чем стандартный 1.5*IQR
  
- **Z-score (строгий)**: |z| > 4
  - Значения, отклоняющиеся более чем на 4 стандартных отклонения

#### 8. Генерация отчетов и экспорт данных
Создание комплексных отчетов в различных форматах:

**Визуализация**:
- Графики распределений данных
- Круговые диаграммы качества данных
- Примеры временных рядов для пациентов

### Frontend

- Веб-интерфейс для визуализации данных
- Интеграция с backend API
- Отображение результатов анализов

### Инфраструктура

- **Docker контейнеризация**: Отдельные контейнеры для backend и frontend
- **Nginx**: Проксирование запросов между frontend и backend
- **Docker Compose**: Оркестрация сервисов
- **Health checks**: Мониторинг состояния контейнеров

---

## Инструкция по развертыванию

Подробные инструкции по развертыванию находятся в директории [`docs/`](docs/README.md):

- **[Локальное развертывание](docs/README_LOCAL.md)** - для разработки и тестирования
- **[Развертывание на хостинге](docs/README_HOSTING.md)** - для продакшена
- **[Docker инструкция](docs/DOCKER_README.md)** - краткая инструкция по Docker
- **[DNS настройка](docs/DNS_ИНСТРУКЦИЯ.md)** - настройка DNS для домена

### Быстрый старт

1. Убедитесь, что установлены Docker и Docker Compose:
```bash
docker --version
docker compose version
```

2. Запустите приложение:
```bash
docker compose up -d
```

3. Приложение будет доступно:
   - Frontend: `http://localhost`
   - Backend API: `http://localhost:8000`
   - API документация: `http://localhost:8000/docs`

---

## Входные и выходные данные

### Входные данные

**Форматы файлов:**
- CSV (`.csv`)
- JSON (`.json`)
- Excel (`.xlsx`, `.xls`)

**Ожидаемые колонки в данных:**
- `subject.subjectGuid` или аналогичная → `patient_id`
- `sample.sampleKitGuid` или аналогичная → `sample_id`
- `subject.biologicalSex` → `sex`
- `subject.birthYear` или `subject.ageAtFirstDraw` → `age`
- `sample.drawYear` или аналогичная → `date`
- Тестовые колонки с префиксами:
  - `am.*` - Anthropometric measures (BMI, height, weight)
  - `chem.*` - Blood chemistry (ALT, albumin, glucose, creatinine)
  - `bc.*` - Blood counts (WBC, RBC, hemoglobin, platelets)
  - `cmv.*` - HCMV Serology (IgG serology)
  - `infl.*` - Inflammatory Markers (HS-CRP, ESR, RF)
  - `lip.*` - Lipid Profile (Cholesterol, triglycerides)

### Выходные данные

**JSON отчет** (`lab_data_analysis_report.json`):
- `schema_suggestions` - маппинг колонок
- `cleaning_report` - отчет об очистке данных
- `stats_by_test` - статистика по каждому тесту
- `provisional_refs` - временные референсные диапазоны
- `flagged_rows` - помеченные строки с отклонениями
- `anomaly_detection` - обнаруженные аномалии
- `outlier_detection` - обнаруженные выбросы
- `sample_dashboard_payload` - пример данных для дашборда

**CSV файлы:**
- `cleaned_lab_data.csv` - очищенные данные с маркировками аномалий
- `cleaned_lab_data_no_outliers.csv` - данные без выбросов
- `anomalies_to_highlight.csv` - только аномалии для подсветки
- `test_statistics.csv` - статистика по каждому тесту
- `provisional_references.csv` - временные референсы в табличном формате
- `suspect_examples.csv` - примеры подозрительных значений
- `outlier_examples.csv` - примеры выбросов

**API ответы:**
- Информация о загруженной таблице (ID, название, размер, колонки)
- Полные данные таблицы с метаданными
- Список всех загруженных таблиц
- Результаты проверки референсных значений

---

## Ключевые аспекты

### Архитектура

- **Микросервисная архитектура**: Разделение на backend, frontend и analytics
- **RESTful API**: Стандартизированный интерфейс для взаимодействия
- **Контейнеризация**: Изоляция сервисов через Docker
- **Хранение данных**: In-memory хранилище для быстрого доступа

### Безопасность

- **CORS**: Настроен для контролируемого доступа
- **Валидация данных**: Проверка форматов и типов данных
- **Обработка ошибок**: Корректная обработка исключительных ситуаций

### Производительность

- **Асинхронная обработка**: Использование FastAPI для высокой производительности
- **Оптимизация запросов**: Эффективная работа с большими объемами данных
- **Health checks**: Мониторинг состояния сервисов

### Важные замечания

⚠️ **ВНИМАНИЕ**: Все референсные диапазоны помечены как `PROVISIONAL` (временные) и созданы статистическими методами. Они **НЕ должны использоваться для клинической диагностики** без замены на официальные референсы от лаборатории.

---

## Обработка

### Этапы обработки данных

1. **Загрузка и парсинг**
   - Определение формата файла
   - Парсинг данных в структурированный формат
   - Валидация структуры данных

2. **Нормализация схемы**
   - Автоматическое определение колонок
   - Маппинг имен колонок к стандартным
   - Поддержка различных форматов входных данных

3. **Очистка данных**
   - Удаление строгих дубликатов
   - Парсинг и валидация дат
   - Приведение числовых полей к корректным типам
   - Обнаружение и маркировка артефактов

4. **Статистический анализ**
   - Расчет базовой статистики (mean, median, std, min, max)
   - Расчет перцентилей (p1, p2.5, p25, p75, p97.5, p99)
   - Расчет межквартильного размаха (IQR)

5. **Создание референсов**
   - Метод 1: Процентили 2.5-97.5 (рекомендуемый)
   - Метод 2: Median ± 2*IQR
   - Метод 3: Mean ± 2*Std (z-score)
   - Поддержка разделения по полу и возрастным группам

6. **Маркировка отклонений**
   - Классификация каждого значения: LOW, NORMAL, HIGH
   - Сравнение с референсными диапазонами
   - Генерация цветовых меток для визуализации

7. **Обнаружение аномалий**
   - **Isolation Forest**: Изоляция аномалий через случайные деревья
   - **DBSCAN**: Кластеризация для выявления точек вне кластеров
   - **Local Outlier Factor (LOF)**: Обнаружение локальных аномалий
   - Консенсус: аномалия, если обнаружена хотя бы двумя методами

8. **Обнаружение выбросов**
   - **IQR (строгий)**: Q1 - 3*IQR, Q3 + 3*IQR
   - **Z-score (строгий)**: |z| > 4
   - Выбросы рекомендуется удалять из данных

9. **Генерация отчетов**
   - Создание JSON отчета с полной информацией
   - Экспорт очищенных данных в CSV
   - Генерация примеров для дашборда

### Алгоритмы обработки

- **Статистические методы**: Описательная статистика, перцентили, IQR, Z-score
- **Методы машинного обучения**: Isolation Forest, DBSCAN, Local Outlier Factor
- **Валидация данных**: Проверка типов, диапазонов, логической согласованности
- **Нормализация**: Приведение данных к единому формату

---

**Версия**: 1.0  
**Последнее обновление**: 2024

