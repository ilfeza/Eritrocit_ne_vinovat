# Локальное развертывание приложения

Инструкция по запуску приложения на локальной машине (Windows, Linux, macOS).

## 📋 Требования

Перед началом убедитесь, что установлены:

- **Docker** версии 20.10 или выше
- **Docker Compose** версии 2.0 или выше

### Проверка установки

```bash
# Проверка Docker
docker --version

# Проверка Docker Compose
docker-compose --version
```

### Установка Docker (если не установлен)

- **Windows/macOS**: [Docker Desktop](https://www.docker.com/products/docker-desktop)
- **Linux**: 
  ```bash
  # Ubuntu/Debian
  curl -fsSL https://get.docker.com -o get-docker.sh
  sudo sh get-docker.sh
  sudo usermod -aG docker $USER
  ```

---

## 🚀 Быстрый старт

### 1. Клонирование/подготовка проекта

```bash
# Если проект в Git репозитории
git clone <your-repo-url>
cd Eritrocit_ne_vinovat

# Или просто перейдите в папку проекта
cd /path/to/Eritrocit_ne_vinovat
```

### 2. Запуск приложения

```bash
# Сборка и запуск контейнеров
docker-compose up -d --build
```

Эта команда:
- Соберет Docker образы для backend и frontend
- Запустит контейнеры в фоновом режиме
- Создаст Docker сеть для связи между контейнерами

### 3. Проверка статуса

```bash
# Проверка статуса контейнеров
docker-compose ps
```

Ожидаемый вывод:
```
NAME                STATUS          PORTS
biodash_backend     Up              0.0.0.0:8000->8000/tcp
biodash_frontend    Up              0.0.0.0:80->80/tcp
```

### 4. Открытие приложения

Откройте в браузере:

- **Фронтенд**: http://localhost/
- **API документация**: http://localhost/api/docs
- **Health check**: http://localhost/health
- **Swagger UI**: http://localhost/api/docs
- **ReDoc**: http://localhost/api/redoc

---

## 📂 Структура проекта

```
Eritrocit_ne_vinovat/
├── back/                    # Backend (FastAPI)
│   ├── app/
│   │   ├── main.py         # Точка входа FastAPI
│   │   ├── routers/        # API роутеры
│   │   ├── services/       # Бизнес-логика
│   │   └── utils/          # Утилиты
│   ├── data/               # Загруженные данные (volume)
│   └── requirements.txt    # Python зависимости
├── front/                  # Frontend (HTML/CSS/JS)
│   ├── index.html
│   └── ...
├── analytics/              # Модуль аналитики
│   ├── data/              # Исходные данные
│   └── output/            # Результаты обработки
├── docker-compose.yml     # Docker Compose конфигурация
├── Dockerfile.backend     # Dockerfile для backend
├── Dockerfile.frontend    # Dockerfile для frontend
└── nginx.conf             # Конфигурация Nginx
```

---

## 🔧 Управление контейнерами

### Просмотр логов

```bash
# Все сервисы
docker-compose logs -f

# Только backend
docker-compose logs -f backend

# Только frontend
docker-compose logs -f frontend

# Последние 100 строк
docker-compose logs --tail=100
```

### Остановка приложения

```bash
# Остановка контейнеров (данные сохраняются)
docker-compose stop

# Остановка и удаление контейнеров
docker-compose down
```

### Перезапуск

```bash
# Перезапуск контейнеров
docker-compose restart

# Перезапуск конкретного сервиса
docker-compose restart backend
```

### Пересборка после изменений

```bash
# Полная пересборка
docker-compose up -d --build

# Пересборка без кэша
docker-compose build --no-cache
docker-compose up -d
```

---

## 🛠️ Разработка

### Режим разработки (без Docker)

Если вы хотите запускать backend локально без Docker:

#### 1. Установка зависимостей

```bash
# Создание виртуального окружения
python -m venv venv

# Активация (Windows)
venv\Scripts\activate

# Активация (Linux/macOS)
source venv/bin/activate

# Установка зависимостей
cd back
pip install -r requirements.txt
```

#### 2. Запуск backend

```bash
# Из папки back/
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

Backend будет доступен на http://localhost:8000

#### 3. Запуск frontend (только Nginx)

```bash
# Запуск только frontend контейнера
docker-compose up -d frontend
```

Или используйте любой локальный веб-сервер для статических файлов.

### Изменение портов

Если порты 80 или 8000 заняты, измените их в `docker-compose.yml`:

```yaml
services:
  frontend:
    ports:
      - "8080:80"  # Измените 8080 на нужный порт
  backend:
    ports:
      - "8001:8000"  # Измените 8001 на нужный порт
```

После изменений:
```bash
docker-compose up -d --build
```

Приложение будет доступно на http://localhost:8080

---

## 🗄️ Работа с данными

### Расположение данных

Данные сохраняются в папке `./back/data/`, которая монтируется как Docker volume.

```bash
# Просмотр загруженных файлов
ls -la back/data/

# Резервное копирование
tar -czf backup-$(date +%Y%m%d).tar.gz back/data/
```

### Очистка данных

```bash
# Удаление всех загруженных файлов
rm -rf back/data/*
# Или на Windows
rmdir /s /q back\data\*
```

---

## 🐛 Решение проблем

### Порт 80 уже занят

**Проблема**: `Error: bind: address already in use`

**Решение**:
1. Найдите процесс, использующий порт 80:
   ```bash
   # Windows
   netstat -ano | findstr :80
   
   # Linux/macOS
   sudo lsof -i :80
   ```
2. Остановите процесс или измените порт в `docker-compose.yml`

### Контейнеры не запускаются

```bash
# Проверьте логи
docker-compose logs

# Проверьте статус
docker-compose ps

# Попробуйте пересобрать
docker-compose down
docker-compose up -d --build
```

### Ошибки при сборке

```bash
# Очистка Docker кэша
docker system prune -a

# Пересборка без кэша
docker-compose build --no-cache
docker-compose up -d
```

### Backend не отвечает

```bash
# Проверьте логи backend
docker-compose logs backend

# Проверьте, что контейнер запущен
docker-compose ps

# Проверьте health endpoint
curl http://localhost/health
```

### Проблемы с правами доступа (Linux)

```bash
# Добавьте пользователя в группу docker
sudo usermod -aG docker $USER

# Перелогиньтесь или выполните
newgrp docker
```

---

## 📊 Мониторинг

### Проверка использования ресурсов

```bash
# Использование ресурсов контейнерами
docker stats

# Информация о контейнерах
docker-compose ps
```

### Health checks

```bash
# Проверка backend
curl http://localhost/health

# Ожидаемый ответ: {"status":"healthy"}
```

---

## 🔄 Обновление приложения

### Обновление кода

```bash
# 1. Остановите контейнеры
docker-compose down

# 2. Обновите код (через git или вручную)
git pull  # если используете Git

# 3. Пересоберите и запустите
docker-compose up -d --build
```

### Обновление зависимостей

Если изменились зависимости в `back/requirements.txt`:

```bash
# Пересборка backend
docker-compose build --no-cache backend
docker-compose up -d
```

---

## 🧹 Очистка

### Удаление контейнеров и образов

```bash
# Остановка и удаление контейнеров
docker-compose down

# Удаление контейнеров, сетей и образов
docker-compose down --rmi all

# Полная очистка (осторожно!)
docker system prune -a --volumes
```

### Сохранение данных перед очисткой

```bash
# Создайте резервную копию
tar -czf backup-$(date +%Y%m%d).tar.gz back/data/

# Затем очистите
docker-compose down --rmi all
```

---

## 📝 Переменные окружения

Можно настроить через `docker-compose.yml`:

```yaml
services:
  backend:
    environment:
      - PYTHONUNBUFFERED=1
      # Добавьте свои переменные здесь
      # - ALLOWED_ORIGINS=http://localhost:3000
```

---

## ✅ Чеклист запуска

- [ ] Docker и Docker Compose установлены
- [ ] Проект склонирован/подготовлен
- [ ] Выполнена команда `docker-compose up -d --build`
- [ ] Контейнеры запущены (`docker-compose ps`)
- [ ] Приложение открывается в браузере (http://localhost/)
- [ ] Health check работает (http://localhost/health)
- [ ] API документация доступна (http://localhost/api/docs)

---

## 📚 Дополнительная информация

- [Развертывание на хостинге](README_HOSTING.md)
- [Основной README](../README.MD)
- [Docker README](DOCKER_README.md)

---

**Версия**: 1.0  
**Последнее обновление**: 2024

